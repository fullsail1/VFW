//window
var tvscreen = Ti.UI.createWindow({
	backgroundColor:"ffffff"
	layout: "horizontal"
});

//loop
for (i=0; i<5; i++){
	var channel = Ti.UI.createView({
		var size = i*10;
var numberOfViews = d    		
		
	backgroundColor: "blue",
	width: 100,
	height: 100,
	left: 10,
	right:0,
	
	
});
		var newChannel=Ti.UI.createLabel({
	color:"#000",
	text: "square = +1,;"
	font:{
		fontSize:typeSize,
		}
})
}
//views


//add
tvscreen.add(channel);
//open
tvscreen.open();

